<header class="header">
  <div class="header-left">
    <a href="/" class="logo">pleasant</a>

    <nav class="nav-links">
      <a href="/products?category=kasketter">kasketter</a>
      <a href="/products?category=tøj">tøj</a>
      <a href="/products?category=livsstil">Livsstil</a>
      <a href="/products?category=drops">drops</a>
    </nav>
  </div>

  <button class="burger" aria-label="Åbn menu">☰</button>

  <div class="header-right">
    <div class="account">
      <span class="icon">👤</span>
      <span>Min profil</span>
    </div>
    <div class="cart">
      <span class="icon">🛒</span>
      <span>Kurv</span>
    </div>
  </div>
</header>

<style>
  .header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 1rem 1.5rem;
    background-color: var(--beige);
    flex-wrap: wrap;
  }

  .header-left {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .logo {
    text-decoration: none;
    color: black;
    font-size: 2rem;
    font-weight: bold;
    margin: 0;
  }

  .burger {
    display: block;
    font-size: 2rem;
    background: none;
    border: none;
    cursor: pointer;
  }

  .nav-links {
    display: none;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1rem;
    width: 100%;
  }

  .nav-links a {
    text-decoration: none;
    color: black;
    font-weight: 500;
    font-size: 1.2rem;
  }

  .nav-links a:hover {
    text-decoration: underline;
  }

  .header-right {
    display: none;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1rem;
    width: 100%;
  }

  span {
    font-family: "felt-tip-woman", sans-serif;
    font-weight: 400;
    font-style: normal;
    font-size: 1.5rem;
  }

  .account,
  .cart {
    display: flex;
    align-items: center;
    gap: 0.3rem;
    cursor: pointer;
  }

  .icon {
    font-size: 1.1rem;
  }

  .nav-links.open,
  .header-right.open {
    display: flex;
  }

  @media (min-width: 768px) {
    .burger {
      display: none;
    }

    .header {
      align-items: center;
      flex-wrap: nowrap;
    }

    .header-left {
      flex-direction: row;
      align-items: baseline;
      gap: 2rem;
    }

    .nav-links {
      display: flex !important;
      flex-direction: row;
      align-items: baseline;
      gap: 1.5rem;
      margin-top: 0;
      width: auto;
    }

    .header-right {
      display: flex !important;
      flex-direction: row;
      gap: 1.5rem;
      margin-top: 0;
      width: auto;
    }
  }
</style>

<script>
  const burger = document.querySelector(".burger");
  const navLinks = document.querySelector(".nav-links");
  const headerRight = document.querySelector(".header-right");

  if (burger && navLinks && headerRight) {
    burger.addEventListener("click", () => {
      navLinks.classList.toggle("open");
      headerRight.classList.toggle("open");
    });
  }
</script>
